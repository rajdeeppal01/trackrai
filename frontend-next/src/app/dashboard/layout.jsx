export const metadata = {
  title: 'Dashboard | TrackrAI',
  description: 'TrackrAI Dashboard - view your application pipeline, metrics, and upcoming interviews.',
  alternates: {
    canonical: '/dashboard',
  }
};

export default function Layout({ children }) {
  return <>{children}</>;
}
