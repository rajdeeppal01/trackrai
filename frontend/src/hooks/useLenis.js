// Smooth scroll hook — lenis removed, using native smooth scroll instead
export default function useLenis() {
  // Native smooth scroll is handled via CSS: scroll-behavior: smooth
  // No external dependency needed
}